메인 캐릭터 동작

1. Rendering 하기 전에
    움직이고 = Move()
        XZ 움직임
            xzMoving == true 면
                이번에 키를 입력받았다는 거니까
                Rotate 를 한다
            
            xzMoving == false 면
                키 입력이 없다, xz 평면에서 움직이지 않는다
                할 거 없음

        Y 움직임
            이전 높이 저장해두고
            ySpeed <- 점프 입력에 의해서 바뀌어 있을 수도 있는 상황
            중력에 의한 속도 변경
        
        XYZ 방향으로 이동

    충돌판정한다
        하나라도 충돌이 있으면
            충돌이 일어난 위치에 정지한다 = yStop()
                ySpeed = 0 으로 만들어 버리고
                땅에 접촉해 있다고 알린다
                    OnGround = true;
                groundHeight 초기화
                이전 높이로 돌아가기
        충돌이 없으면
            올라가든, 내려가든 공중에 있는 상황
            OnAir()
                OnGround = false;
                현재 위치를 파악해서, groundHeight 보다 내려가는 지 체크한다
                    더 내려가면, 실시간으로 메인 캐릭터의 높이가 groundHeight
                    카메라가 이후 메인 캐릭터를 따라 내려가게 된다



2. 점프 동작

    스페이스 바를 입력받으면 => ySpeed 를 증가시킨다
    공중에 떠 있는 동안에는 점프할 수 없다

3. 대쉬 동작

    앞으로 달려드는 동작, 버튼을 따로 만들자
    대시 동작은 공중에서도 할 수 있지만
    한번 시작하면 땅에 닿을 때까지 연속해서 할 수 없다
        Dashing 변수로 관리
        땅에 닿으면 초기화
    대시 버튼이 눌리면 
        frontVec 방향으로 추가 속도를 얻고
        ySpeed 도 조금 얻는 방식으로 진행하면 될듯?