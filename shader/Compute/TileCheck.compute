#version 460 core
layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;


struct Tile
{
    vec4 position;
    vec4 collAndTime;
};

// SSBO
layout(std430, binding = 1) buffer TileBuffer
{
    Tile tileData[];
};



void main()
{
    vec3 temp = gl_GlobalInvocationID;

    tileData[gl_GlobalInvocationID.x].position.x = gl_GlobalInvocationID.x;
}