1. Compute Shader 이용해서 Floor 들의 상태를 계산하기
   1. Tile Sturct 생성
      1. vec4 (position, 메인 캐릭터의 충돌 유무 -> output 용도)
      2. vec4 (collision, disappear, save time, limit time)
   2. SSBO 버퍼 생성
      1. Tile 의 배열
   3. Tile 배열의 값을 SSBO 버퍼에 넣기
   4. Compute Program 생성 => Uniform Varible 관리
      1. current time
      2. xyz scale
      3. 전체 타일의 개수
      4. 메인 캐릭터의 위치
   5. Compute Shader 작성
      1. 한 개의 그룹은 32 개의 스레드로 구성해야 한다 => nVidia, 32 threads = 1 warp
      2. 대신 y 축으로는 스레드 1개만 사용하고, x, z 방향으로 이용
      3. 충돌판정, 시간 체크 등 동작을 수행
         1. 메인 캐릭터와의 충돌 판단
            1. 충돌 시, time 세팅
            2. 메인 캐릭터가 충돌했다는 것을 알림


2. GPU Instancing 을 이용해서 그려내기